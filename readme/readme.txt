plugin for CudaText.
Adds items to plugins menu to format SQL text (with "SQL" lexer, you may correct lexer name in file settings/plugins.json).
Uses sqlparse lib:
  https://code.google.com/p/python-sqlparse/

Author: Alexey T.
